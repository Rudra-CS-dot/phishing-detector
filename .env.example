# Copy this file to .env and fill in your key to enable live threat-intel lookups.
# Get a free key: https://developers.google.com/safe-browsing/v4/get-started
# The app works fine without this — it just skips the live API check and
# relies on the rule-based engine alone.
SAFE_BROWSING_API_KEY=
